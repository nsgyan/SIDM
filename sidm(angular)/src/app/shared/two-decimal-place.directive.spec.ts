import { TwoDecimalPlaceDirective } from './two-decimal-place.directive';

describe('TwoDecimalPlaceDirective', () => {
  it('should create an instance', () => {
    const directive = new TwoDecimalPlaceDirective();
    expect(directive).toBeTruthy();
  });
});
