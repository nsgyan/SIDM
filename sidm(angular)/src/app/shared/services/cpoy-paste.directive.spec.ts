import { CpoyPasteDirective } from './cpoy-paste.directive';

describe('CpoyPasteDirective', () => {
  it('should create an instance', () => {
    const directive = new CpoyPasteDirective();
    expect(directive).toBeTruthy();
  });
});
